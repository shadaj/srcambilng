package srcambilng

object Examples {
	val paragraph = """According to research at Cambridge University, it
						|doesn't matter in what order the letters in a word are,
						|the only important thing is that the first and last 
						|letters are in the right place.""".stripMargin('|')
}